#include <bits/stdc++.h> // C++의 표준 라이브러리가 포함된 헤더파일
using namespace std; // std라는 네임스페이스를 사용

int main(){
  char a = 'a';
  cout << (int)a  << "\n";
  cout << (int)a - 97 << "\n";
  cout << (int)a - 'a' << "\n";
  
  
  return 0;
}